# Leer un número positivo, calcular su cuadrado y su cubo. Imprimir los resultados.
numero = float(input('Ingrese un número: '))
if numero < 0:
 print('El número que ha ingresado es negativo')
 quit()
else:
 print(f'El cuadrado de', numero, 'es:', numero**2)
 print(f'El cubo de ', numero, 'es:', numero**3)
